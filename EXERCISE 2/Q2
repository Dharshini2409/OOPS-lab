class simpleintrest {
    public static void main(String args[])
    {
        float P = 100, R = 10, T = 2;
        float SI = (P * T * R) / 100;
        System.out.println("Simple interest = " + SI);
    }
}
